return {
  "ibhagwan/fzf-lua",
  opts = {
    winopts = {
      fullscreen = true,
      preview = {
        layout = "flex",
        flip_columns = 250, -- cols to switch to horizontal on flex
      },
    },
  },
}
