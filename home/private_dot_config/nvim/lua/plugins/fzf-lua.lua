return {
  "ibhagwan/fzf-lua",
  opts = {
    winopts = {
      fullscreen = true,
      preview = {
        layout = "flex",
        flip_columns = 200, -- cols to switch to horizontal on flex
      },
    },
  },
}
